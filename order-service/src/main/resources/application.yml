server:
  port: 8082
spring:
  application:
    name: order-service
  # Configuración de Postgres
  datasource:
    url: jdbc:postgresql://localhost:5432/projects
    username: postgres
    password: t0m03
    driver-class-name: org.postgresql.Driver

  # JPA / Hibernate
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # Cconfiguracion de eventos (Spring Cloud Stream)
  cloud:
    stream:
      bindings:
        orderPlaced-out-0:
          destination: order-placed-topic
      kafka:
        binder:
          brokers: pkc-619z3.us-east1.gcp.confluent.cloud:9092
          configuration:
            security.protocol: SASL_SSL
            sasl.mechanism: PLAIN
            sasl.jaas.config: 'org.apache.kafka.common.security.plain.PlainLoginModule required username="FIMMW65ZNRONMZZ2" password="cflt6PJ/pCBMdjHHDJrCzfjk7KgNGRo4+AVLdlW/JXRZNH8r7b/ajRcQbmutvGQw";'

# Configuración de Actuator
management:
  endpoints:
    web:
      exposure:
        include: "health,info"
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.ecommerce.order_service: DEBUG
    org.springframework.cloud.stream: DEBUG
    org.springframework.kafka: DEBUG